import 'package:flutter/material.dart';

class CardDatabase extends StatefulWidget {
  const CardDatabase({Key? key}) : super(key: key);

  @override
  _CardDatabaseState createState() => _CardDatabaseState();
}

class _CardDatabaseState extends State<CardDatabase> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
